//! **Iteration #8: Contador de repeticiones**

// Crea una función que nos devuelva el número de veces que se 
// repite cada una de las palabras que lo conforma.  Puedes usar este array para probar tu función:
//? Este tengo que volver a hacerlo

const counterWords = [
    'code',
    'repeat',
    'eat',
    'sleep',
    'code',
    'enjoy',
    'sleep',
    'code',
    'enjoy',
    'upgrade',
    'code'
  ];

  const result={};
  repeatCounter(counterWords);
  function repeatCounter(arrayCounterDuplicates) {
    for (let index = 0; index < arrayCounterDuplicates; index++) {
        for (let j = 0; j < arrayCounterDuplicates; j++) {
            if (arrayCounterDuplicates[i] in result) {
                // Lo de arriba ignifica si arraycounter.. esta dentro del onjeto resultado
            } else {
                
            }
            
        }
        
    }
  }